# Tools Instructions

- Tooling scripts must be deterministic and cross-platform.
- Prefer TypeScript plus Node standard library for repo automation.
- Scripts that orchestrate commands should emit structured audit data.
- Keep automation logs in ignored artifact paths, not committed source files.
