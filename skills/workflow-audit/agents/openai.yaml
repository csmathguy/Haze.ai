interface:
  display_name: "Workflow Audit"
  short_description: "Audit logging for agent workflows"
  default_prompt: "Use $workflow-audit when a change needs audited workflow logging and deterministic validation in this repo."
policy:
  allow_implicit_invocation: true
