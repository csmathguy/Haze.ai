interface:
  display_name: "UI Design Workflow"
  short_description: "Design rules for the local tax app"
  default_prompt: "Use $ui-design-workflow to design or refine a frontend experience in this tax app repo."
policy:
  allow_implicit_invocation: true
