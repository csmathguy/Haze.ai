interface:
  display_name: "Implementation Workflow"
  short_description: "Repo workflow for safe code changes"
  default_prompt: "Use $implementation-workflow to implement a change in this tax app repo."
policy:
  allow_implicit_invocation: true
